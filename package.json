{
  "name": "seleniumdemo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=development node test/*.js",
    "stage": "NODE_ENV=stage node test/*.js",
    "prod": "NODE_ENV=production node test/*.js",
    "test:dev": "NODE_ENV=development mocha --no-timeouts --parallel --reporter mochawesome --require mochawesome/register",
    "test:stage": "NODE_ENV=stage mocha --no-timeouts --parallel --reporter mochawesome --require mochawesome/register",
    "test:prod": "NODE_ENV=production mocha --no-timeouts --parallel --reporter mochawesome --require mochawesome/register"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.8",
    "dotenv": "^16.3.1",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.11.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.48.0",
    "eslint-config-standard": "^17.1.0",
    "eslint-plugin-import": "^2.28.1",
    "eslint-plugin-n": "^16.0.2",
    "eslint-plugin-promise": "^6.1.1",
    "eslint-plugin-react": "^7.33.2"
  }
}